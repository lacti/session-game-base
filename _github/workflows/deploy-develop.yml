name: "Deploy develop"
on:
  push:
    branches:
      - develop
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: "Clone this repository"
        uses: actions/checkout@v1

      - name: "Deploy front-end"
        run: cd front && yarn && yarn deploy
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
          AWS_DEFAULT_OUTPUT: ${{ secrets.AWS_DEFAULT_OUTPUT }}
          REACT_APP_AUTH_URL: ${{ secrets.DEV_REACT_APP_AUTH_URL }}
          REACT_APP_GAME_APPLICATION_ID: ${{ secrets.DEV_REACT_APP_GAME_APPLICATION_ID }}
          REACT_APP_LOBBY_URL: ${{ secrets.DEV_REACT_APP_LOBBY_URL }}
          S3_BUCKET_NAME: ${{ secrets.DEV_S3_BUCKET_NAME }}
          S3_BUCKET_PATH: ${{ secrets.DEV_S3_BUCKET_PATH }}
          DISTRIBUTION_ID: ${{ secrets.DEV_DISTRIBUTION_ID }}

      - name: "Deploy back-end"
        run: cd api && yarn && yarn deploy
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
          AWS_DEFAULT_OUTPUT: ${{ secrets.AWS_DEFAULT_OUTPUT }}
          REDIS_HOST: ${{ secrets.DEV_REDIS_HOST }}
          REDIS_PASSWORD: ${{ secrets.DEV_REDIS_PASSWORD }}
          STAGE: ${{ secrets.DEV_STAGE }}
          WS_ENDPOINT: ${{ secrets.DEV_WS_ENDPOINT }}
